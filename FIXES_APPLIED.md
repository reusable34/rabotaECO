# Исправления и доработки

## Что было исправлено

### 1. Ошибка с `responsible_person`
- Исправлена валидация в модели `Client` - поле теперь опциональное
- Миграция создана: `m240101_000011_add_responsible_person_to_clients.php`

### 2. Вкладки теперь работают
- Все 8 вкладок переключаются и показывают соответствующий контент:
  - ✅ Карта требований
  - ✅ Артефакты/хранилище
  - ✅ Календарь событий
  - ✅ Риски
  - ✅ Изменения в законодательстве (заглушка)
  - ✅ Договора/счета/акты
  - ✅ Снижение затрат (заглушка)
  - ✅ Шаблоны (заглушка)

### 3. Кнопки экспорта/импорта CSV работают
- **Экспорт требований** - скачивает CSV файл со всеми требованиями
- **Загрузить CSV** - открывает диалог выбора файла (импорт будет доработан)

### 4. Единая страница требований
- Все разделы интегрированы в одну страницу `/requirements`
- Переключение между разделами через вкладки
- Фильтры работают для пересчета требований

## Что нужно сделать

### 1. Применить миграцию БД

```bash
cd backend
php yii migrate
```

Это добавит поле `responsible_person` в таблицу `clients`.

### 2. Перезапустить серверы (если нужно)

```bash
# Backend
docker-compose restart backend

# Frontend
cd frontend
npm run dev
```

## Структура страницы требований

Теперь страница `/requirements` содержит:

1. **Шапка** с кнопками экспорта/импорта CSV
2. **Фильтры** для пересчета требований
3. **Вкладки** для переключения между разделами
4. **Контент** в зависимости от выбранной вкладки

## Функционал

- ✅ Экспорт требований в CSV
- ✅ Загрузка CSV (базовая версия)
- ✅ Переключение вкладок
- ✅ Просмотр требований в таблице
- ✅ Просмотр артефактов/документов
- ✅ Просмотр календаря событий
- ✅ Просмотр рисков
- ✅ Просмотр договоров

## Примечания

- Разделы "Изменения в законодательстве", "Снижение затрат" и "Шаблоны" пока содержат заглушки
- Импорт CSV требует доработки на backend для сохранения данных
- Все данные загружаются с сервера через API
